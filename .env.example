# --- FRONTEND (VITE) ---
VITE_API_URL="https://your-worker-url.workers.dev/api"
VITE_GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
FRONTEND_URL="https://your-site.com"

# --- BRIDGE & NGROK (LOCAL) ---
NGROK_AUTHTOKEN="your-ngrok-token"
WA_BRIDGE_URL="https://your-domain.ngrok-free.dev"
WA_BRIDGE_KEY="your-secret-key"

# --- BACKEND / WORKER (CLOUDFLARE) ---
# For deployment, use: wrangler secret put <KEY>
MP_ACCESS_TOKEN="your-mp-token"
D1_DB_NAME="your-db-name"

# --- LEGACY TESTS ---
# (Defunt but kept for record if needed)
TEST_USER_EMAIL="admin@test.com"
